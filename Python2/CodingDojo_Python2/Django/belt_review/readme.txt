'''
-- Instructions for installing Django-Extensions --
you will need to pip install the django-extensions once.

$ pip install django-extensions


after installing the django
after installing the apps/__init__.py/urls.py/templates/etc

install django extensions
google django extensions
click on the one from django-extensions

Before running these commands, make sure your in the main app leve with the manage.py and apps
run these command in the terminal:
$ git clone git://github.com/django-extensions/django-extensions.git
$ cd django-extensions
$ python setup.py install

scroll down to the Contents and click on ~For usage
Goto the configuration.  INSTALLED_APPS=
input the 'django_extensions',

-now you can wipe the data from the database.
run in terminal:
python manage.py reset_db

-search django extensions commands for more.



'''
