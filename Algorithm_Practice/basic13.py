# print 1-255
def print1to255:
    num = 1
    while num <= 255:
        print num
        num = num + 1

# or
for count in range(1,256):
    print count

# print odd 1-255
def printodd1to255:
    oddArray = []
    while num <= 255
    # complete this
# or
def printodd1to255
    num = 1
    while num < 255
        print num
        num += 2

# print int and sum 0-255
def printIntSum0to255:
    sum = 0
    num = 0
    while num <= 255:
        sum += num
        print "New number:" + num + "Sum:" + sum

# or



#  iterate and print array
for x in range(0, len(arr)):
    print arr[x]

# find and print max
def printArrayMax:
    if len(arr)==0:
        print "Empty array, no max value"
        return
    max = arr[0]
    idx = 1
    for idx in arr:
        if arr[idx] > max:
            max = arr[idx]
            print "Max value is :" + max

#  get and print average
count = 0
sum = 0
while count < len(arr):
    sum += arr[count]
    count += 1
avg = sum/len(arr)
print avg

# array with odds


# square the values


# greater than Y


# zero out negative numbers


# min, max, average


#  shift array values
idx = 1
while idx
# finish this

# swap string for array negative values


# min
for x in range(0, len(arr):
    min = arr[0]
    if arr[x]<min:
        min = arr[x]
